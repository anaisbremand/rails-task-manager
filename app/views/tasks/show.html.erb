<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<ul> <%= link_to "Back to list", tasks_path %> </ul>
<ul> <%= link_to "Edit task", task_edit_path(@task) %></ul>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
